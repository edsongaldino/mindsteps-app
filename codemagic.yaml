# codemagic.yaml

workflows:
  ios-release:
    name: MindSteps iOS

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - flutter pub get
      - cd ios && pod install
      - cd ..
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa