workflows:
  ios-release:
    name: MindSteps iOS

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build ios --release --no-codesign

    artifacts:
      - build/ios/**/*